T9 GROOM PATHOLOGICAL VERIFICATION LOOP (first of remediation phase): 56min/422 turns/433 tools/41.7M tokens vs branch tip d71fc11 = AC-1 ONLY, zero commits and zero file writes 13+min, attention fired twice (bash 241s+240s at path >). Diagnosis: lane burning budget on per-block verification iterations (each AC expected-red proof + mutation falsification) WITHOUT committing intermediate work - the exact failure my T10 brief saw (ceiling died before commits); T10 survived via commit-FIRST discipline, T9 is dying by verification-before-commit (write order inverted). INTERVENED: hard-deadline steer - close-out <=12 min: commit what exists, ONE runacs pass, push, comment honest per-AC state table + NEEDS-GROOM-RESUME markers for unfinished blocks; explicitly told unfinished-but-honest = SUCCESS (anti-perfectionism frame; the loop reads as trying to satisfy every AC bar at once). LESSON for remaining 21 issue briefs: MANDATORY commit-every-2-ACs AND if one block loops min, mark NEEDS-GROOM-RESUME and move on escape hatch stated at dispatch (T10 got the ceiling warning; T9 proves loops need an explicit exit-per-block, not just per-file). If close-out lands partial: round-2 resume completes marked blocks (ceiling-era machinery, zero-loss).
