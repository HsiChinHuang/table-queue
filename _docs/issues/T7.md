---
id: T7
title: Docs hygiene sweep (owner-approved T6/T7/T10 from the debt register)
depends: []
platform_issue: 59
---

## Why this exists

Executes the owner-approved tickets T6/T7/T10 from `_docs/debt-register.md` (branch `docs/PM-debt-register`). The debt register itself is not on main yet; this issue file is the local mirror of Platform #59 and is groomed by PM before dispatch.

## Scope (verbatim from Platform #59)

(1) own/complete `_docs/debt-register.md` (PM becomes author of record); (2) B-11.md DoD/Metadata 11->14 + drop nonexistent conftest fixture names (D7); (3) testing.md 7-vs-8 test-file count; (4) CONTRIBUTING.md Constraints convention: `services/*` allowance when ACs force file collision + how to extend a Constraints file list without touching a frozen AC (D9); (5) main.py comment chars: replace the 9 inherited non-ASCII codepoints (U+2011 x8, U+2013 x1) with ASCII in comments only, zero code change, spot-run pair pytest after (D5/T10).

## Non-goals

No AC text edits except B-11 paperwork lines; no behavior change anywhere; no label edits.

## Gate (verbatim from Platform #59)

runacs B-08 --only AC-14 and B-11 --only AC-14 stay green; CJK/CR scan 0; ruff clean on main.py after char swap; ls-remote == HEAD.

## Note for the groomer (measured 2026-09-12)

Scope item (5) is already satisfied on main: `backend/app/main.py` contains zero non-ASCII codepoints at commit `0d99402`. Verify before re-doing it, and re-scope the AC accordingly. Also note the 2026-09-12 process-doc restructure deleted `_docs/team/roles.md` (replaced by `_docs/team/INDEX.md` + `_docs/team/orchestrator-*.md`); `_docs/documents.md` and `_docs/project-guide.md` were repaired in the same commit that registered this issue, and closed issue ACs (P0-10, D-09) still name `roles.md` as historical measurement prose.

## Dependencies

None (docs-only, no app-code dependency).
