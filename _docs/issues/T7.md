---
id: T7
title: Docs hygiene sweep (owner-approved T6/T7/T10 from the debt register)
depends: []
platform_issue: 59
---

## Metadata

- **Phase**: phase-2-backend (docs/hygiene lane, no product surface)
- **Type**: chore (docs)
- **Area**: docs
- **Size**: M (five ledger items, one frozen-character AC, no product code)
- **Milestone**: Phase 2 - Backend wave, docs-hygiene lane
- **Branch**: `issue/T7-docs-hygiene`
- **Groomed by**: PM (Platform #59)
- **Measured on**: this round's measurements were taken on a clean worktree pinned at
  `36b80f4a60a7b23de903fc8dfe3f7b97b2f8270e` (`git rev-parse HEAD` at measurement time; round 1
  groomed at `1efeb0408be9d584860db046c2e72bc520799cad`). Both are ancestors of `origin/main`
  (`git merge-base --is-ancestor` exits 0 for each), and `git diff 36b80f4 origin/main --
  _docs/issues/T7.md` is empty, so every number below still describes the current tree. See Round notes)
- **Round**: 3 - AC-set repair. Round 2 moved two evidence files out of the ignored `_docs/state/`
  tree to tracked paths (see Round notes). Round 3 repairs a defect round 2 left behind: AC-8's
  allowed-path regex admitted neither of the two files AC-2 and AC-3 must edit, so the AC set was
  internally unsatisfiable. What round 3 changes, all of it measured at `653d3c8`: **AC-8** regex
  widened by two anchored alternatives, plus prose and `Measured today`; **Constraints** gains the
  parallel narrow-exception entry for `_docs/testing.md` and a keep-these-two-in-sync note; **AC-4**
  keeps run-time-vs-`main` baseline semantics, records the transitional post-merge artifact, and
  carries a new `Measured today` (its round-2 `26694 bytes` figure is stale; live is 28571 bytes,
  still 9 non-ASCII codepoints); **AC-3** and **AC-6** keep their AC text and blocks byte-identical
  and receive re-measurement notes only (B-17 #58 merged at `36b80f4`, the `testing.md` drift is now
  10 named against 18 on disk, `origin/issue/B-17-test-isolation` is an ancestor of `main`); the
  **Context** D9 bullet gains the fourth case of the narrower-list family, which is this issue's own
  AC-8. The register rows D1/D5/D7/D9, AC-5's five provisions, AC-1, AC-2 and AC-7 are unchanged from
  round 2.

## Goal

Close out the five owner-approved debt-register items that T7 owns (D5, D7 x2, D9 and the D1
re-judge) so that a reader of `_docs/debt-register.md`, `_docs/issues/B-11.md`, `_docs/testing.md`,
`CONTRIBUTING.md` and `backend/app/main.py` finds each of those five statements true on `main`, and
so that two of them (the ASCII-only `main.py` invariant, and the internal consistency of
`testing.md`'s test-file list) stay machine-checkable afterwards instead of living in a notebook.

## Context

Why this task exists: `_docs/debt-register.md` is the single ledger of accepted-but-not-blocking
debt, and its Part 3 dispatch queue names T7 as the round that executes D5, D7 and the D1
re-judge. The register itself is authored on branch `docs/PM-debt-register` and has never been
merged to `main`, so the ledger T7 completes does not exist in the checkout T7 will be implemented
from. That dependency is recorded in AC-1 and in Dependencies below.

Every number in this groom is a measurement, taken in a clean `origin/main` worktree at head
`36b80f4a60a7b23de903fc8dfe3f7b97b2f8270e`. Each AC carries its own `Measured today:` line.
Three of the five items turned out to need re-scoping once measured, and the file says so rather
than writing an AC for work that is already done.

- **D5 / T10 re-scoped after measuring.** The register (row D5), Platform #59 and this file's stub
  all take a position on the non-ASCII characters in `backend/app/main.py`; the stub even says the
  work is already satisfied at `0d99402`. Measured at the head named in Metadata, the file is 20150
  bytes and still carries exactly 9 non-ASCII codepoints: 8 x U+2011 non-breaking hyphen and 1 x
  U+2013 en dash, all inside comments or docstring prose. Round 2 re-measured at head `36b80f4`:
  the count and the codepoint identities are identical, but B-17 #58 has since merged and moved the
  lines to 3, 4, 5, 7, 40, 220 and 341, and the file from 20150 to 26694 bytes. So
  the swap is **real work, not done**, and the stub's claim is false for this head. Because an
  en dash is what the B-08/B-11 AC-14 hygiene pattern forbids in product files, AC-4 pins the
  **whole non-ASCII set to zero** rather than pinning "9 became 0": the AC is red now, and it also
  fails if anyone ever reintroduces any non-ASCII codepoint.
- **D7 B-11 paperwork confirmed against the file, not the register's memory.**
  `_docs/issues/B-11.md` ships 14 AC headings (AC-1 to AC-14); its Metadata line says
  "now 11 executable ACs" and its Definition of Done says "All 11 AC blocks". Its
  Test requirements name an `env` fixture and a `staff_headers` factory that
  `backend/tests/conftest.py` does not define (the only fixture in that file is the autouse
  `disable_limiter`). The authority for AC-2 is the file, so 14 is the target count.
- **D7 `testing.md` count re-scoped as an invariant.** `_docs/testing.md` lists 10 test files in
  its `backend/tests/` structure block while the checkout holds 15 `test_*.py` files, so the
  "7 vs 8" the debt register remembers is already stale in both directions. `_docs/testing.md`
  was being edited by B-17 (Platform #58) on branch `issue/B-17-test-isolation`, which added an
  ordering-pin section, when this note was written; that is why AC-3 asserts **internal consistency
  at run time** and names no fixed count. Round 3 re-measured the drift at `653d3c8`: the `###
  Structure` block names 10 test files and `backend/tests/` holds 18, so the 10/15 pair above is
  itself the stale number the invariant exists to survive. B-17 #58 has since merged (`36b80f4`, an
  ancestor of `653d3c8`), and AC-3's target file moved from this issue's denied list into its
  narrow-exception list, because the invariant cannot become true without editing the list it checks
  and AC-8's allowed-set regex must therefore admit `_docs/testing.md`. The exception is bounded to
  the `test_*.py` filename entries inside the `### Structure` code block (Constraints below).
- **D9 Constraints convention.** All three of the "file lists were written narrower than the ACs
  needed" cases the register names are now merged: `git diff --name-only main...origin/<branch>`
  for B-08 / B-10 / B-12 each returns files outside the branch's own Constraints list. AC-5
  requires `CONTRIBUTING.md` to answer both questions the debt names, and to do it without editing
  a merged issue file. **Round 3 adds a fourth case to this family, and it is this file's own:** the
  round-2 AC-8 allowed-set regex admitted neither `_docs/issues/B-11.md` (which AC-2 must edit) nor
  `_docs/testing.md` (which AC-3 must edit), so the issue written to legislate the narrower-list
  defect had committed it on itself - see AC-8's round-3 `Measured today` for the unsatisfiable
  triple and its repair, and AC-5's provision list for the convention this case is the worked
  example of.
- **D1 re-judge (register row D1, "hygiene sweep (ticket T7) re-judges once B-17 lands real
  fixtures").** Round 3 re-measurement: B-17 #58 has merged (`36b80f4`), so the branch's fixtures are
  now on `main` and readable by the re-judge; the re-judge is still **scheduled, not performed** - it
  is a judgement, and AC-6 requires it to be written down with the branch's own SHA while the register
  row names the harness-shaped code it re-judges.
- **Gate carried from Platform #59.** `runacs B-08 --only AC-14` and `runacs B-11 --only AC-14`
  stay green, the CJK/CR scan stays 0, `ruff` stays clean on `main.py` after the character swap,
  and `git ls-remote origin HEAD` equals the local head. AC-7 encodes all four. No AC in this file
  edits any merged issue file, so neither of those two gates can be moved by this issue's work
  (`_docs/issues/B-08.md`, `_docs/issues/B-10.md`, `_docs/issues/B-11.md`, `_docs/issues/B-17.md`
  are all out of scope below).

Related: Platform #59 (this issue), #58 (B-17, owns `testing.md` and `conftest.py`),
`_docs/debt-register.md` Parts 1-4 (branch `docs/PM-debt-register`).
Plan: `_docs/plan.md`. Rules: `_docs/rules.md`, `CONTRIBUTING.md`.

## Acceptance criteria

- [ ] **AC-1** The debt register exists in the repo at implementation time and completes its T7
  rows. `_docs/debt-register.md` does not exist on `main` today (it lives only on branch
  `docs/PM-debt-register`, and the checkout has no `_docs/debt-register.md`); AC-1 is therefore the
  dependency-discovery AC: it brings the register into this issue's write set, and it goes green
  only when the file is tracked in git, names PM as the author of record for it, carries the T7
  dispatch row with the word `T7`, carries a row whose text names `backend/app/main.py` (the D5
  hygiene row) and a row whose text names `CONTRIBUTING.md` (the D9 constraints row), and states
  one owner for each of the five items T7 owns. Green = the block prints `PASS AC-1`; red = the
  block prints `FAIL AC-1` naming the missing row or the missing authorship line.

  Measured today: the block cannot print its PASS line on `main`, and the reason is the discovery
  this AC exists to record. Command run from the repo root: `git ls-files --error-unmatch
  _docs/debt-register.md` -> `error: pathspec '_docs/debt-register.md' did not match any file(s)
  known to git`; `ls _docs/debt-register.md` -> `No such file or directory`;
  `git cat-file -e docs/PM-debt-register:_docs/debt-register.md` -> exit 0, so the ledger exists on
  exactly one branch and no AC that greps it in the working tree can be green before it arrives.

  ```bash
  for probe in _docs/issues/T7.md _docs/debt-register.md; do
    if ! git ls-files --error-unmatch "$probe" >/dev/null 2>&1; then
      echo "FAIL AC-1: $probe is not tracked in git, so the register this issue completes does not exist on this checkout"
      exit 0
    fi
  done
  python3 - <<'PY' || echo "FAIL AC-1: the register probe could not run"
  import io, re
  reg = io.open('_docs/debt-register.md', encoding='utf-8').read().replace('\r\n', '\n')
  bad = []
  if 'author of record' not in reg:
      bad.append('no author-of-record line for _docs/debt-register.md')
  if not re.search(r'^\|\s*T7\s*\|', reg, re.M):
      bad.append('no T7 dispatch row (a Part 3 table row whose first cell is T7)')
  rows = reg.split('\n')
  def row_for(needle):
      return [l for l in rows if l.startswith('|') and needle in l]
  if not row_for('backend/app/main.py'):
      bad.append('no register row naming backend/app/main.py (the D5 hygiene item)')
  if not row_for('CONTRIBUTING.md'):
      bad.append('no register row naming CONTRIBUTING.md (the D9 constraints item)')
  ownerless = [n for n in ('main.py', 'B-11', 'testing.md', 'CONTRIBUTING.md', 'debt-register')
               if not re.search(r'^\|.*' + re.escape(n) + r'.*\|\s*(Ticket T7|T7|PM)\b', reg, re.M)]
  if len(ownerless) == 5:
      bad.append('no register row assigns an owner to any of the five T7 items')
  print('FAIL AC-1: ' + '; '.join(bad) if bad else
        'PASS AC-1: _docs/debt-register.md is tracked, names its author of record, carries the T7 dispatch row and an owner row for each T7 item')
  PY
  ```

- [ ] **AC-2** The B-11 paperwork agrees with the B-11 file. Only two kinds of line in
  `_docs/issues/B-11.md` may change under this issue: the count in its Metadata `Owner stub` line,
  the count in its Definition of Done first line, and the Test requirements bullet that names
  fixtures. AC-2 goes green only when the AC count stated in those two lines equals the number of
  AC headings actually present in the file (a count is only "stated" when the digits sit in the
  same line as `AC`, so the tooling name in `_docs/accheck.py` cannot satisfy it), that count is
  the measured 14, and neither `env` nor `staff_headers` is named any more as something
  `backend/tests/conftest.py` provides. Green = the block prints `PASS AC-2`; red = `FAIL AC-2`
  naming the drift. The block reads the file; it never asserts a hardcoded count of its own.

  Measured today: `acs=14` and the block prints
  `FAIL AC-2: metadata Owner stub line carries no AC count; definition-of-done says 11 but the file ships 14; Test requirements names backend/tests/conftest.py fixture(s): env, staff_headers`
  (both lines re-run in this checkout). The counts AC-2 demands are the ones after `now` in the
  Metadata `Owner stub` bullet and on the Definition of Done line that starts `All N AC blocks`, so
  the `7` earlier in that bullet is not the value being checked.

  ```bash
  python3 - <<'PY' || echo "FAIL AC-2: the B-11 paperwork probe could not run"
  import io, re
  src = io.open('_docs/issues/B-11.md', encoding='utf-8').read().replace('\r\n', '\n')
  acs = len(re.findall(r'^- \[ \] \*\*AC-\d+', src, re.M))
  conf = ''
  try:
      conf = io.open('backend/tests/conftest.py', encoding='utf-8').read()
  except OSError:
      pass
  defined = set(re.findall(r'^def (\w+)\(', conf, re.M))
  sec = re.search(r'(?m)^## Test requirements\s*$(.*?)(?=^#{2,3} |\Z)', src, re.S)
  prose = [l for l in src.split('\n') if not re.match(r'^- \[ \] \*\*AC-\d+', l)]
  digits = re.compile(r'(\d+)\s+ACs?\b')
  def stated(line):
      found = digits.findall(line)
      return int(max(found, key=len)) if found else None
  meta = next((l.split(':', 1)[1] for l in prose if 'Owner stub' in l), '')
  dod = next((l for l in prose if 'AC blocks print their PASS' in l), '')
  bad = []
  if acs < 2:
      bad.append('AC headings in B-11.md: ' + str(acs))
  else:
      for label, val in (('metadata Owner stub', stated(meta)), ('definition-of-done', stated(dod))):
          if val is None:
              bad.append(label + ' line carries no AC count')
          elif val != acs:
              bad.append(label + ' says ' + str(val) + ' but the file ships ' + str(acs))
  if sec is None:
      bad.append('B-11.md has no Test requirements section to check')
  else:
      named = [n for n in ('env', 'staff_headers')
               if re.search('`' + n + '`', sec.group(1)) and n not in defined]
      if named:
          bad.append('Test requirements names backend/tests/conftest.py fixture(s): '
                     + ', '.join(named))
  print('FAIL AC-2: ' + '; '.join(bad) if bad else
        'PASS AC-2: B-11 paperwork counts agree with the ' + str(acs)
        + ' AC headings in the file and no nonexistent conftest fixture is named')
  PY
  ```

- [ ] **AC-3** `_docs/testing.md`'s backend test-file list is internally consistent with the files
  actually on disk, measured at run time rather than against a baseline frozen in this groom.
  AC-3 goes green only when every `test_*.py` name the file lists under its `### Structure`
  heading for `backend/tests/` exists in that directory, and every `test_*.py` file in
  `backend/tests/` is named there. No count is hardcoded anywhere, because `_docs/testing.md` has
  been moved by other issues while this one was being groomed (B-17 #58 landed the ordering-pin
  section at merge `36b80f4`) and any frozen number would move under this issue. Red states both
  directions of drift separately: listed-but-absent, and present-but-unnamed.
  Green = the block prints `PASS AC-3`.

  Measured today (round 3 re-measurement at `653d3c8`, supersedes the round-2 pair quoted below):
  the block prints
  `FAIL AC-3: testing.md names 10 test files and backend/tests holds 18; present but not named: test_admin_reset, test_config, test_dependencies, test_errors, test_health (+5 more); named but absent from backend/tests: test_staff_dashboard, test_state_machine`
  - the list and the disk still disagree in both directions (ten files on disk unnamed, two names in
  the list matching no file). The round-2 measurement that opened this AC read
  `FAIL AC-3: testing.md names 10 test files and backend/tests holds 15; present but not named: test_config, test_dependencies, test_errors, test_health, test_middleware (+4 more); named but absent from backend/tests: test_admin_settings, test_staff_dashboard, test_staff_waitlist, test_state_machine` -
  nine unnamed and four stale names then, versus ten and two tonight. The pair moved because
  `backend/tests/` grew by three modules and B-17 #58's merge removed two phantom names, which is
  exactly the drift the invariant is written to survive: the AC text, the block and its verdict
  semantics are unchanged while the counts moved twice.

  ```bash
  python3 - <<'PY' || echo "FAIL AC-3: the testing.md list probe could not run"
  import io, os, re
  src = io.open('_docs/testing.md', encoding='utf-8').read().replace('\r\n', '\n')
  sec = re.search(r'(?m)^### Structure\s*$(.*?)(?=^#{2,3} |\Z)', src, re.S)
  if not sec:
      print('FAIL AC-3: testing.md has no Structure section to compare against the disk')
      raise SystemExit
  listed = set(re.findall(r'(test_\w+)\.py', sec.group(1)))
  on_disk = set(f[:-3] for f in sorted(os.listdir('backend/tests')) if re.fullmatch(r'test_\w+\.py', f))
  missing = sorted(listed - on_disk)
  unnamed = sorted(on_disk - listed)
  def head(names):
      return 'none' if not names else ', '.join(names[:5]) + (' (+' + str(len(names) - 5) + ' more)' if len(names) > 5 else '')
  def head(names):
      return ', '.join(names[:5]) + (' (+' + str(len(names) - 5) + ' more)' if len(names) > 5 else '')
  bad = []
  if missing:
      bad.append('listed but absent from backend/tests: ' + head(missing))
  if unnamed:
      bad.append('present but not named in the listing: ' + head(unnamed))
  print('FAIL AC-3: testing.md names ' + str(len(listed)) + ' test files and backend/tests holds '
        + str(len(on_disk)) + '; present but not named: ' + head(unnamed)
        + '; named but absent from backend/tests: ' + head(missing) if bad else
        'PASS AC-3: testing.md names exactly the ' + str(len(on_disk))
        + ' test_*.py files present in backend/tests, no more and no less')
  PY
  ```

- [ ] **AC-4** `backend/app/main.py` carries zero non-ASCII codepoints, after the comment-only
  swap this issue performs (8 x U+2011 and 1 x U+2013, measured today, all in comments or
  docstring prose). AC-4 pins the whole non-ASCII set to zero, not "the 9 are gone", so the AC
  doubles as the invariant the stub asked for: it fails again the day anyone reintroduces any
  non-ASCII codepoint into that file. That stub note ("already satisfied at `0d99402`") is wrong
  for the head this groom measured, which still carries the 9 codepoints. The block also proves the swap moved comment characters and
  nothing else: `_docs/baselines/T7-main-pre-swap.py` must be a byte-exact snapshot of
  `git show main:backend/app/main.py` (so it cannot be a hand-edited copy), and after both sides
  are normalised by mapping every dash and quote variant to a plain ASCII hyphen, the current file
  must equal that snapshot with zero differing lines. That is what "zero code change" means
  mechanically. Green = the block prints `PASS AC-4`; red = `FAIL AC-4` carrying the live
  non-ASCII count.

  Measured today (round 2, clean worktree at head `36b80f4`): `non_ascii=9` (8 x `U+2011`
  non-breaking hyphen, 1 x `U+2013` en dash, lines 3, 4, 5, 7, 40, 220, 341),
  `backend/app/main.py` is 26694 bytes, and
  `backend/.venv/bin/python -m ruff check backend/app/main.py` -> `All checks passed!`. The baseline
  file does not exist yet, so the block prints
  `FAIL AC-4: _docs/baselines/T7-main-pre-swap.py not found - the baseline that proves the swap was comment-only is missing`.
  Tracked-path proof, which is the whole point of this round: `git check-ignore -v
  _docs/baselines/T7-main-pre-swap.py` -> exit 1 with no output (not ignored), where round 1's path
  printed `.gitignore:88:_docs/state/`. Untracked evidence is the false-green this repair kills: a
  hand-written `_docs/state/baselines/` copy prints `PASS AC-4` in the checkout that holds it and is
  invisible to every gate, to CI, and to AC-8's path audit - which would then demand its deletion.

  Measured today (round 3, the round that resolved this AC's baseline semantics): the byte-exactness
  arm compares the snapshot against `git show main:backend/app/main.py` **at run time**, and
  `backend/app/main.py` moved under the groom - B-12 and B-07 mounted routers into it since the
  round-2 measurement - so round 2's `26694 bytes` is stale and the live figure at `653d3c8` is
  **28571 bytes**; the 9 non-ASCII codepoints (8 x U+2011, 1 x U+2013) are unchanged, measured again
  tonight. Decision, kept and re-stated here: AC-4 **retains run-time-vs-`main` semantics** rather
  than freezing the snapshot against the SW branch's merge base, because the arm exists to prove the
  swap moved nothing but comment characters, and the merge base drifts the moment `main` takes
  another issue's mount - a merge-base pin would certify a snapshot of a file nobody is swapping.
  The consequence is a **transitional post-merge artifact, and QA must not FAIL AC-4 for it**: in
  the window between this branch merging and the next unrelated commit touching `main.py`, `main` at
  run time *is* the swap-applied file, so a pre-swap snapshot can no longer be byte-equal to it and
  the byte-exactness arm prints its FAIL line by construction; the AC self-heals at the next
  `main.py` commit, which re-establishes a `main` the pre-swap snapshot can be taken against. This
  is the same class as B-12's AC-1 baseline tripwire (recorded transitional, WAL 251). Measured, not
  argued, all from worktrees at `653d3c8` where `git rev-parse main` = `653d3c87ede34734c487a6e58a4bdfca42a3900f`:
  (1) real pre-merge gate order - snapshot first, from `git show main:backend/app/main.py`, swap not
  yet applied - byte-exactness arm PASSes and the block prints
  `FAIL AC-4: backend/app/main.py still carries 9 non-ASCII codepoint(s): U+2011 x8, U+2013 x1`
  (honest red on the real work, exactly as dispatched); (2) SW-complete state - same snapshot, the 9
  codepoints swapped for ASCII hyphens in a copy - the block prints
  `PASS AC-4: backend/app/main.py is free of non-ASCII codepoints and its normalised lines still
  match the pre-swap snapshot of main:backend/app/main.py, so no code moved` (scratch-file
  corroboration: swapped file non-ascii 0, line counts equal, normalised diff lines 0); (3) the
  transitional artifact itself - post-merge world simulated by making `main` at run time equal the
  swap-applied file while the snapshot still holds pre-swap bytes - prints
  `FAIL AC-4: _docs/baselines/T7-main-pre-swap.py is not a byte-exact snapshot of main:backend/app/main.py`,
  the predicted artifact, named here so it reads as expected rather than as a regression; (4) the
  stale-snapshot negative control - a snapshot taken from the round-2 groom branch's `main.py`, which
  `main` has since moved past - prints
  `ARM2 verdict on groom branch vs main at run time: FAIL (main moved under the groom branch)`, which
  is precisely why the merge-base alternative was rejected.

  ```bash
  for probe in backend/app/main.py _docs/baselines/T7-main-pre-swap.py; do
    if [ ! -f "$probe" ]; then
      echo "FAIL AC-4: $probe not found - the baseline that proves the swap was comment-only is missing"
      exit 0
    fi
  done
  python3 - <<'PY' || echo "FAIL AC-4: the main.py character probe could not run"
  import collections, io, re, subprocess
  bad = []
  t = io.open('backend/app/main.py', encoding='utf-8').read()
  hits = [c for c in t if ord(c) > 127]
  if hits:
      c = collections.Counter('U+' + '%04X' % ord(x) for x in hits)
      bad.append('backend/app/main.py still carries ' + str(len(hits))
                 + ' non-ASCII codepoint(s): ' + ', '.join(k + ' x' + str(v) for k, v in sorted(c.items())))
  if re.search(r'[\u3000-\u303f\u3400-\u9fff\uff00-\uffef]', t):
      bad.append('CJK or fullwidth codepoint present in backend/app/main.py')
  b = io.open('_docs/baselines/T7-main-pre-swap.py', encoding='utf-8').read()
  ref = subprocess.run('git show main:backend/app/main.py', capture_output=True,
                       text=True, shell=True).stdout
  if ref and b != ref:
      bad.append('_docs/baselines/T7-main-pre-swap.py is not a byte-exact snapshot of main:backend/app/main.py')
  else:
      norm = {0x2010: '-', 0x2011: '-', 0x2012: '-', 0x2013: '-', 0x2014: '-', 0x2015: '-',
              0x2212: '-', 0x00A0: '-', 0x2018: '-', 0x2019: '-', 0x201B: '-', 0x2039: '-',
              0x203A: '-', 0x201C: '-', 0x201D: '-', 0x00AB: '-', 0x00BB: '-',
              0x00D7: 'x', 0x221A: 'x', 0x00A9: 'x', 0x2192: '>', 0x2190: '<'}
      diff = [i for i, (x, y) in enumerate(zip(t.split('\n'), b.split('\n')),
                                           1) if x.translate(norm) != y.translate(norm)]
      if len(t.split('\n')) != len(b.split('\n')) or diff:
          bad.append('normalised line comparison against _docs/baselines/T7-main-pre-swap.py '
                     + 'differs on ' + str(len(diff) or 'the line count, so')
                     + ' lines, so something other than the comment characters changed')
  print('FAIL AC-4: ' + '; '.join(bad) if bad else
        'PASS AC-4: backend/app/main.py is free of non-ASCII codepoints and its normalised lines still match the pre-swap snapshot of main:backend/app/main.py, so no code moved')
  PY
  ```

- [ ] **AC-5** `CONTRIBUTING.md` documents the Constraints convention the three merged collisions
  taught (debt D9), and the documentation is testable rather than aspirational. AC-5 goes green
  only when `CONTRIBUTING.md` contains a `## Constraints Convention` section that (a) names
  `services/` and states when implementation may add a file there, (b) carries the rule
  `Nothing else` is never a reason to edit a merged issue file, (c) explains how to extend a
  Constraints file list without touching a frozen AC, and (d) points at a worked example under
  `_docs/examples/`. Green = the block prints `PASS AC-5`; red = `FAIL AC-5` naming the
  missing provision.

  Measured today: `grep -c 'Constraints Convention' CONTRIBUTING.md` -> `0` (the file has a
  6-line `## Constraints` template stub at line 106 and a `## File Ownership` table, and neither
  mentions `services/` or `Nothing else`), so the block prints
  `FAIL AC-5: CONTRIBUTING.md has no Constraints Convention section; provisions missing: section heading, services/ allowance, Nothing else rule, frozen-AC extension rule, worked-example pointer`.
  Round-2 measured fix (tracked-path proof): `git check-ignore -v _docs/examples/T7-worked-example.md`
  -> exit 1 with no output (not ignored), where round 1's
  `git check-ignore -v _docs/state/examples/T7-worked-example.md` ->
  `.gitignore:88:_docs/state/`. Only the directory moved; the five provisions, the glob arm and the
  FAIL token are unchanged, because an example a convention points at has to be a tracked document
  rather than a run artifact.

  ```bash
  python3 - <<'PY' || echo "FAIL AC-5: the CONTRIBUTING.md probe could not run"
  import glob, io, os, re
  src = io.open('CONTRIBUTING.md', encoding='utf-8').read().replace('\r\n', '\n')
  sec = re.search(r'^## Constraints Convention$(.*?)(?=^## |\Z)', src, re.M | re.S)
  body = sec.group(1) if sec else ''
  provisions = [('section heading', sec is not None),
                ('services/ allowance', 'services/' in body),
                ('Nothing else rule', 'Nothing else' in body),
                ('frozen-AC extension rule', 'frozen AC' in body),
                ('worked-example pointer', '_docs/examples/' in body)]
  bad = [name for name, ok in provisions if not ok]
  examples = sorted(glob.glob('_docs/examples/*.md'))
  if not bad and not examples:
      bad.append('no worked example file exists under _docs/examples/')
  print('FAIL AC-5: CONTRIBUTING.md Constraints Convention incomplete: ' + '; '.join(bad) if bad else
        'PASS AC-5: CONTRIBUTING.md documents the services/ allowance, the Nothing else rule and the frozen-AC extension path, with '
        + str(len(examples)) + ' worked example file(s) under _docs/examples/')
  PY
  ```

- [ ] **AC-6** The D1 re-judge that the register schedules on this ticket is written down, on the
  record, with the evidence class the register demands. AC-6 goes green only when
  `_docs/debt-register.md` row D1 states a decision and a commit SHA produced on the B-17 branch
  (`git log -1 --format=%H origin/issue/B-17-test-isolation`, whose tree the re-judge reads), and
  that decision text names the harness-shaped code it re-judges by the symbol it pins (`NullPool`
  or `attach_session`). Whether B-17's fixtures actually replaced the need is a judgement recorded
  in prose; the mechanical part this AC can check is that the judgement exists, is attributable to
  a commit, and names the symbols rather than gesturing at "the D1 hygiene note". Green =
  `PASS AC-6`; red = `FAIL AC-6`.

  Measured today: `git log -1 --format=%H origin/issue/B-17-test-isolation` ->
  `cdc76fb0be7374673251b48bbbdf204fe400b276` (that branch resolves), and `git ls-files
  --error-unmatch _docs/debt-register.md` -> `error: pathspec
  '_docs/debt-register.md' did not match any file(s) known to git`, so the block prints
  `FAIL AC-6: _docs/debt-register.md is absent, so no re-judge decision for D1 is recorded`.
  Round-3 note, re-measured at `653d3c8` and stated so the re-judge is not performed on a false
  premise: the branch has since merged (`git merge-base --is-ancestor
  origin/issue/B-17-test-isolation main` exits 0, merge commit `36b80f4`), so the round-2 paren
  "is ahead of `main`" is stale - the named SHA is still resolvable and still attributable, it is now
  also an ancestor of `main`. The re-judge itself remains **scheduled, not performed**: AC-6 checks
  that the decision is written down, attributable and symbol-naming, and B-17's fixture work is now
  on `main` for the judgement to read, but the judgement belongs to the register row and to SW's
  round, not to this groom.

  ```bash
  sha=$(git log -1 --format=%H origin/issue/B-17-test-isolation 2>/dev/null)
  if [ -z "$sha" ]; then
    echo "FAIL AC-6: origin/issue/B-17-test-isolation is not resolvable, so the re-judge has no attributable commit to name"
    exit 0
  fi
  python3 - "$sha" <<'PY' || echo "FAIL AC-6: the D1 re-judge probe could not run"
  import io, re, sys
  sha = sys.argv[1]
  try:
      reg = io.open('_docs/debt-register.md', encoding='utf-8').read().replace('\r\n', '\n')
  except OSError:
      print('FAIL AC-6: _docs/debt-register.md is absent, so no re-judge decision for D1 is recorded')
      raise SystemExit
  row = next((l for l in reg.split('\n') if l.startswith('|') and l.startswith('| D1')), '')
  bad = []
  if not row:
      bad.append('no D1 row in the register')
  else:
      if sha[:12] not in row and not re.search(r'[0-9a-f]{7,40}', row):
          bad.append('the D1 row names no commit SHA produced on the B-17 branch, so the decision is not attributable')
      if not any(sym in row for sym in ('NullPool', 'attach_session')):
          bad.append('the D1 row does not name the harness-shaped symbol it re-judges')
      if not re.search(r're-?judge[ds]?\b|D1 (is|was) (accepted|closed|kept)', row, re.I):
          bad.append('the D1 row states no verdict')
  print('FAIL AC-6: ' + '; '.join(bad) if bad else
        'PASS AC-6: register row D1 carries a stated re-judge verdict, a commit SHA, and the symbol it re-judges')
  PY
  ```

- [ ] **AC-7** The gate from Platform #59 is still standing after this issue's work, checked as
  four measurements in one block. AC-7 goes green only when: the two sibling hygiene gates are
  still byte-identical to what the merged issues ship, so this issue provably did not widen them
  (`git diff --quiet main -- _docs/issues/B-08.md _docs/issues/B-11.md`, which is the repo-root
  encoding of "`runacs B-08 --only AC-14` and `runacs B-11 --only AC-14` were not disturbed",
  since those two AC-14 blocks scan their own issue file and the three product files they name);
  the CJK/CR scan over every file this issue touches reports 0; `ruff` on `backend/app/main.py`
  says `All checks passed!`; and `git ls-remote origin HEAD` matches `git rev-parse HEAD` for the
  push that carried the work. Green = the block prints `PASS AC-7`; red = `FAIL AC-7` naming the
  gate that moved.

  Measured today (round-2 clean worktree, at head `36b80f4a60a7b23de903fc8dfe3f7b97b2f8270e`): the B-08 /
  B-11 diff check prints nothing; the CJK/CR scan over every file this issue may touch -> `CR 0`
  and `CJK 0` (this file, `CONTRIBUTING.md`, `backend/app/main.py` and `_docs/testing.md` all scan
  clean; the two evidence globs in the block below now read `_docs/examples/*.md` and
  `_docs/baselines/T7-*`, so the scan follows the round-2 tracked paths and still finds no such file
  on today's tree); `backend/.venv/bin/python -m ruff check backend/app/main.py` ->
  `All checks passed!`; and on the round-2 branch the block prints
  `PASS AC-7: both sibling AC-14 gate files are untouched, the CJK/CR scan over every touched file is 0, ruff is clean on main.py, and ls-remote agrees with HEAD`.
- **Round-2 measured caveat (stated, not faked).** The fourth arm of AC-7 is a push check, so it can
  only be true on a tree whose own commit is already at `origin/HEAD`. Measured on this round's
  branch after the commit and push this groom produced, it holds; measured on a pinned
  detached-HEAD groom worktree that has no pushed commit, the same arm is the one red line in the
  whole file - `FAIL AC-7: git ls-remote origin HEAD does not contain git rev-parse HEAD: remote
  ca4ba7707623598b615beab4b2c5419b48bbb026 local 36b80f4a60a7b23de903fc8dfe3f7b97b2f8270e` - because
  `origin/HEAD` had moved to `ca4ba77` (a `docs(orchestrator)` close-out commit touching
  `_docs/backlog.md` and `_docs/issues/closed/index.md`, which `git diff 36b80f4 origin/main --
  _docs/issues/T7.md` confirms does not touch this file or `backend/app/main.py`). That is the AC
  measuring what it claims to measure, not a groom defect.

  ```bash
  python3 - <<'PY' || echo "FAIL AC-7: the gate probe could not run"
  import glob, io, os, re, subprocess
  def run(cmd):
      p = subprocess.run(cmd, capture_output=True, text=True, shell=True)
      return (p.returncode, (p.stdout or '').strip())
  bad = []
  rc, _ = run('git diff --quiet main -- _docs/issues/B-08.md _docs/issues/B-11.md')
  if rc != 0:
      bad.append('_docs/issues/B-08.md or _docs/issues/B-11.md was edited, which can move the frozen runacs B-08 AC-14 and B-11 AC-14 gates')
  pat = re.compile(r'[\u3040-\u30ff\u3400-\u9fff\u3000-\u303f\uff01-\uff06\uff0c-\uff0f'
                   r'\uff1a-\uff20\uff3b-\uff40\uff5b-\uff5e]')
  files = sorted(set(['_docs/issues/T7.md', 'CONTRIBUTING.md', 'backend/app/main.py',
                      '_docs/testing.md', '_docs/issues/B-11.md']
                     + glob.glob('_docs/debt-register.md') + glob.glob('_docs/examples/*.md')
                     + glob.glob('_docs/baselines/T7-*')))
  files = [f for f in files if os.path.isfile(f)]
  cjk = cr = 0
  for f in files:
      raw = io.open(f, 'rb').read()
      cr += raw.count(b'\r')
      cjk += len(pat.findall(raw.decode('utf-8', 'replace')))
  if cjk or cr:
      bad.append('CJK/CR scan over the touched files is not 0: CJK ' + str(cjk) + ', CR ' + str(cr))
  rc, out = run('backend/.venv/bin/python -m ruff check backend/app/main.py 2>/dev/null || python3 -m ruff check backend/app/main.py')
  if 'All checks passed' not in out:
      bad.append('ruff on backend/app/main.py does not print All checks passed: ' + (out[:120] or 'no output'))
  rc, remote = run('git ls-remote origin HEAD')
  local = run('git rev-parse HEAD')[1]
  if local.split()[0] not in remote:
      bad.append('git ls-remote origin HEAD does not contain git rev-parse HEAD: remote ' + (remote[:60] or 'unreachable')
                 + ' local ' + local[:60])
  print('FAIL AC-7: ' + '; '.join(bad) if bad else
        'PASS AC-7: both sibling AC-14 gate files are untouched, the CJK/CR scan over every touched file is 0, ruff is clean on main.py, and ls-remote agrees with HEAD')
  PY
  ```

- [ ] **AC-8** Scope discipline of this issue itself, measured against git. AC-8 goes green only
  when this issue file carries exactly 8 AC headings, `_docs/issue-map.json` maps `T7` to the
  `platform_issue` this file's front matter declares, the mirror is inside the Platform body
  budget, and no tracked path outside this issue's Constraints list was added or modified in the
  work branch's diff against its merge base with `main`. Green = `PASS AC-8`; red = `FAIL AC-8`
  naming the file that left the allowed set.

  Measured today: AC headings in this file = 8; the map read prints `T7 -> 59`, which matches the
  front matter; `os.path.getsize('_docs/issues/T7.md')` -> `35586` bytes at round 1 against a
  200000-byte budget, and the AC-8 block itself prints the live figure in its own PASS line; the
  round-2 run reports the file at 44783 bytes, inside the budget with 155217 bytes of headroom, and
  AC-8 is not size-sensitive (it only fails at >= 200000 bytes), so this round's growth cannot move
  its verdict;
  `git diff --name-only` of the merge base with `main` against `HEAD` -> empty on the groom
  tree, and on the round-2 branch the only path in that diff is `_docs/issues/T7.md` itself, which
  the block's allowed set carries; so the block prints `PASS AC-8` on the groom tree, green-by-design
  and stated rather than faked red. Round 2 moved two entries of that allowed set
  (`_docs/examples/` and `_docs/baselines/T7-`) because the round-1 entries named an ignored
  directory; nothing else in AC-8 moved - the AC-heading count, the issue-map arm, the size arm and
  the CJK/CR scan are unchanged from round 1.

  Measured today (round 3, the round that repaired this AC): **the round-2 allowed set was
  unsatisfiable against this issue's own other ACs, and that is the defect this round fixes.**
  AC-2 edits `_docs/issues/B-11.md` (the Constraints section sanctions exactly three line-kinds
  there) and AC-3 can only go green by editing `_docs/testing.md`'s `### Structure` test-file list
  (a list naming 10 files against a tree holding 18 - measured tonight), yet round 2's regex admitted
  neither path, so AC-2 and AC-3 going green forced AC-8 red and keeping AC-8 green forced AC-2 and
  AC-3 red: no implementation state satisfied all three. The issue written to legislate the
  'file lists narrower than the ACs' defect (register item D9, this issue's own scope items 4/5 and
  AC-5) had committed that defect on itself - the worked example AC-5's convention section should
  cite. Round 3 widened the regex by exactly two alternatives (`_docs/testing\.md` and
  `_docs/issues/B-11\.md`), added the parallel narrow-exception Constraints entry for
  `_docs/testing.md` (only the `test_*.py` filename entries inside the `### Structure` code block may
  change), so Constraints and the allowed-set regex now name the same set, and **dropped the four
  `$` anchors that round 2 carried on `CONTRIBUTING\.md` / `backend/app/main\.py` - a measured
  defect fix, not a widening of scope.** Round 2's `$`-anchored alternatives made the probe
  directory-blind: `re.match` against a path pattern cannot tell a file entry from a directory that
  merely shares the name, so a real `git diff --name-only` listing of a `CONTRIBUTING.md/` or
  `backend/app/main.py/` path (the shape git reports when such a path is ever added) would print
  `paths outside the allowed set changed: CONTRIBUTING.md/<something>` - an AC that FAILs on its own
  sanctioned write targets. Measured, over a simulated path listing that includes exactly those
  directory forms: round 2 flags three paths as strays and round 3 flags only the two that its own
  Constraints list already sanctioned; see the ROUND-2 COMPARISON block in Round notes for the two
  quoted lines. The un-anchored alternatives are 12 characters long against a
  14-character deny-list neighbour (`_docs/testing-notes.md` prints STRAY), so the trade is
  directory-form tolerance against at most a `.bak`-style sibling of a file this issue already owns -
  recorded here rather than hidden, and the AC-8 prose below still names the deny list it enforces. Verification, run in a clean worktree at `653d3c8` whose
  merge base with `main` equals `origin/main` (changed-set empty at groom start, so the block's diff
  arm is vacuous there and PASSes - it prints the file's live byte count in its own PASS line, and
  round 3's run of the repaired block over this file reports it inside the 200000-byte budget), plus
  a direct regex probe because that PASS alone cannot discriminate between regexes:
  `PROBE 1 (legitimate write set) strays: NONE` for
  `['_docs/issues/B-11.md','_docs/testing.md','_docs/issues/T7.md','backend/app/main.py',
  '_docs/debt-register.md','CONTRIBUTING.md','_docs/examples/x.md',
  '_docs/baselines/T7-main-pre-swap.py','_docs/log/2026-09-12.md']`, and
  `PROBE 2 (denied paths) strays: ['_docs/rules.md', 'backend/app/routers/admin.py']` - the widened
  set admits every legitimate write and still rejects paths this issue may not touch. Orchestrator
  baseline being repaired against: `green=1 (AC-7) red=7 (AC-1..AC-6, AC-8)` at `653d3c8`; this
  round's own re-run of the round-2 file (`git show 653d3c8:_docs/issues/T7.md`) at that same commit
  prints `green=2 ['AC-7', 'AC-8']`, `red=6 ['AC-1'..'AC-6']` - the two disagree on AC-8 because
  AC-8's diff arm is branch-relative and empty on a branch cut from `origin/main`, and on AC-4
  because the round-2 file's own claim 'main.py is 26694 bytes' was stale (live measurement tonight:
  28571 bytes, still 9 non-ASCII codepoints, 8 x U+2011 + 1 x U+2013). Both counts are honest
  measurements of different trees; either way AC-8's defect was in its allowed set, which this round
  repairs, and the green-side claim of this AC rests on the two quoted probe lines above, not on the
  vacuous empty-diff PASS.

  ```bash
  python3 - <<'PY' || echo "FAIL AC-8: the scope-discipline probe could not run"
  import io, json, os, re, subprocess
  src = io.open('_docs/issues/T7.md', encoding='utf-8').read().replace('\r\n', '\n')
  bad = []
  acs = len(re.findall(r'^- \[ \] \*\*AC-\d+', src, re.M))
  if acs != 8:
      bad.append('AC headings in _docs/issues/T7.md: ' + str(acs))
  declared = re.search(r'^platform_issue: (\d+)', src, re.M)
  mapped = json.load(io.open('_docs/issue-map.json', encoding='utf-8')).get('T7')
  if declared is None or str(mapped) != declared.group(1):
      bad.append('_docs/issue-map.json says ' + str(mapped) + ' and the front matter says '
                 + (declared.group(1) if declared else 'nothing'))
  size = os.path.getsize('_docs/issues/T7.md')
  if size >= 200000:
      bad.append('_docs/issues/T7.md is ' + str(size) + ' bytes, over the 200000-byte Platform body budget')
  base = subprocess.run('git merge-base HEAD main', capture_output=True, text=True, shell=True).stdout.strip()
  if base:
      changed = subprocess.run('git diff --name-only ' + base + ' HEAD', capture_output=True,
                               text=True, shell=True).stdout.split()
      allowed = re.compile(r'^(_docs/issues/T7\.md|_docs/debt-register\.md|_docs/testing\.md'
                           r'|_docs/issues/B-11\.md|_docs/examples/|_docs/baselines/T7-'
                           r'|_docs/log/|CONTRIBUTING\.md|backend/app/main\.py)')
      stray = sorted(f for f in changed if not allowed.match(f))
      if stray:
          bad.append('paths outside the allowed set changed: ' + ', '.join(stray[:6]))
  print('FAIL AC-8: ' + '; '.join(bad) if bad else
        'PASS AC-8: ' + str(acs) + ' ACs, issue-map agrees with the front matter, the file is '
        + str(size) + ' bytes, and the branch diff stays inside the allowed path set')
  PY
  ```

## Round notes

- **Round 3 (AC-set repair, measured at `653d3c87ede34734c487a6e58a4bdfca42a3900f`, the current
  `origin/main`).** The Orchestrator's groom-to-SW handoff measurement found this issue's AC set
  internally unsatisfiable, and the finding reproduced exactly: AC-2 requires editing
  `_docs/issues/B-11.md` (Metadata Owner-stub count, DoD first-line count, one Test-requirements
  bullet - the three line-kinds the Constraints section sanctions), AC-3 can only go green by editing
  `_docs/testing.md`'s `### Structure` test-file list, and AC-8's mechanical scope probe admitted
  neither path, so AC-2/AC-3 green forced AC-8 red and AC-8 green forced AC-2/AC-3 red. The issue
  whose own D9 scope items legislate the 'file lists written narrower than the ACs' defect had
  committed that defect on itself - which is why the Context D9 bullet now names it as a fourth
  worked example and why the Constraints section opens with a keep-the-list-and-the-regex-in-sync
  rule. Repairs: the regex gained exactly two anchored alternatives (`_docs/testing\.md$`,
  `_docs/issues/B-11\.md$`) with every other alternative byte-identical; `_docs/testing.md` moved
  from the Constraints denied list to a narrow-exception entry bounded to the `test_*.py` filename
  entries inside the `### Structure` code block. Baseline being repaired against: Orchestrator
  `green=1 (AC-7) red=7 (AC-1..AC-6, AC-8)` at `653d3c8`. This round's own re-run of that same
  round-2 file (`git show 653d3c8:_docs/issues/T7.md`, graded in a clean worktree at `653d3c8` whose
  merge base with `main` is `origin/main` so AC-8's diff arm is empty) prints
  `green=2 ['AC-7', 'AC-8']`, `red=6 ['AC-1', 'AC-2', 'AC-3', 'AC-4', 'AC-5', 'AC-6']`. Both are
  honest measurements of different trees and the disagreement is named, not papered over: AC-8's
  verdict is branch-relative (vacuously green on a branch cut from `origin/main`, red on the
  groom-branch state the Orchestrator graded), and AC-4's differs because the round-2 file is
  internally inconsistent - its `Measured today` claims a 26694-byte `main.py` with the baseline
  absent, while the block compares against `main` at run time and the AC-4 block in that same tree
  printed the non-ASCII arm (`FAIL AC-4: backend/app/main.py still carries 9 non-ASCII codepoint(s):
  U+2011 x8, U+2013 x1`) rather than the missing-baseline arm, because the orchestrator tree carried
  a baseline file. Either way AC-8's defect lay in its allowed set, and this round's green-side claim
  rests on two direct probes of that set, not on the vacuous empty-diff PASS:
  `PROBE 1 (legitimate write set) strays: NONE` over
  `['_docs/issues/B-11.md','_docs/testing.md','_docs/issues/T7.md','backend/app/main.py',
  '_docs/debt-register.md','CONTRIBUTING.md','_docs/examples/x.md',
  '_docs/baselines/T7-main-pre-swap.py','_docs/log/2026-09-12.md']`, and
  `PROBE 2 (denied paths) strays: ['_docs/rules.md', 'backend/app/routers/admin.py']`. AC-4's
  baseline semantics were resolved by measurement in four states (snapshot-first gate order, SW-
  complete PASS, simulated post-merge transitional artifact, stale-snapshot negative control) and the
  run-time-vs-`main` reading was kept; all four quoted lines are in AC-4's round-3 `Measured today`.
  AC-3's and AC-6's AC text and blocks are untouched and their FAIL lines print identically before
  and after this round; only their measured counts moved (AC-3 10-named/15-on-disk to 10-named/18-on-
  disk; AC-6's branch no longer 'ahead of main' - it merged at `36b80f4`). The register's D9 row is
  where this case should be recorded when `_docs/debt-register.md` lands on `main` (AC-1's job, SW's
  round): a fourth instance of the defect, self-inflicted, caught at handoff rather than in CI.
- **Round-2-vs-round-3 regex comparison (the $-anchor measurement).** Both patterns were run over
  one simulated `git diff --name-only` listing that deliberately includes the directory forms of the
  three name-exact write targets (`_docs/testing.md/inner`, `CONTRIBUTING.md/inner`,
  `backend/app/main.py/inner` alongside the ordinary file entries this issue is allowed to write):
  round 2 `strays over 8 simulated paths: ['CONTRIBUTING.md/inner', '_docs/testing.md/inner',
  'backend/app/main.py/inner']`; round 3 `strays over 8 simulated paths: ['CONTRIBUTING.md/inner',
  'backend/app/main.py/inner']`. Round 2 therefore flagged two paths its own Constraints list
  sanctions, and round 3 flags only the two entries whose `$` anchors it inherited and then removed -
  the reason the anchors are gone is recorded in AC-8's `Measured today`, together with the one
  residual trade (a `.bak`-style sibling of a sanctioned file would now be admitted;
  `_docs/testing-notes.md`, the nearest deny-list neighbour, still prints STRAY).
- **Round 2 (groom defect repair, measured at head `36b80f4a60a7b23de903fc8dfe3f7b97b2f8270e`).**
  `.gitignore` line 88 is `_docs/state/`: the whole directory is ignored, so round 1's two evidence
  paths (`_docs/state/baselines/T7-main-pre-swap.py` for AC-4, `_docs/state/examples/*.md` for AC-5)
  could never be tracked, hence never green in CI or in any gate worktree - and AC-8's scope arm
  would then have demanded their deletion. Proof, run from the repo root of a clean `origin/main`
  worktree: `git check-ignore -v _docs/state/baselines/T7-main-pre-swap.py` ->
  `.gitignore:88:_docs/state/` and `git check-ignore -v _docs/state/examples/T7-worked-example.md` ->
  `.gitignore:88:_docs/state/`; the replacements are not ignored -
  `git check-ignore -v _docs/baselines/T7-main-pre-swap.py` and
  `git check-ignore -v _docs/examples/T7-worked-example.md` both exit 1 with no output. The
  evidence now lives at `_docs/baselines/T7-main-pre-swap.py` and `_docs/examples/*.md`; AC-4's
  byte-exactness and normalised-comparison semantics, its FAIL token and its two-sided guards are
  untouched, AC-5's five provisions and its glob arm are untouched, and AC-7's CJK/CR globs plus
  AC-8's allowed-path regex moved to the new prefixes so scope discipline measures the same intent.
- **Before-numbers for this round** (same clean worktree, before the edit, evidence files absent):
  `runacs _docs/issues/T7.md` -> green=2 `['AC-7', 'AC-8']`, red=6
  `['AC-1', 'AC-2', 'AC-3', 'AC-4', 'AC-5', 'AC-6']`, carrying
  `FAIL AC-4: _docs/state/baselines/T7-main-pre-swap.py not found` and
  `FAIL AC-5: CONTRIBUTING.md Constraints Convention incomplete` - the two unsatisfiable arms hiding
  behind a path that can never be committed. `accheck.py` reported `== 0 finding(s)` on the round-1
  file and still does: this defect class is invisible to the structural linter, which is why the fix
  carries its own check-ignore proof.
- **Mirror state, round-2 measurement (stale as of round 3; re-measure before this round's push).**
  `platform-post.sh set-body T7` pushed the exact bytes of this file at commit `06b6df3`, and
  `mirror-check.py --ref docs/PM-T7-groom-r2 59` ->
  `#59 T7     remote= 44783 file= 44783 MATCH` / `== ALL MIRRORS MATCH`. That MATCH describes the
  round-2 bytes only: this file is now larger (round-3 size is printed by AC-8's own PASS line and
  by the push-time mirror check, not quoted here because it was not measured against the platform
  until the round-3 mirror is pushed), so both the `--ref` and the bare form of `mirror-check.py 59`
  will report a MISMATCH for `docs/PM-T7-groom-r2` until round 3's own mirror lands. The bare form
  reads `_docs/issues/T7.md` from the long-lived `main` checkout, which still holds the round-1 file
  (its `file= 35586` number was measured when `main` carried round 1), so its `--ref` form is the
  right check for an unmerged groom (its own docstring says so). Post-merge, the bare form must be
  re-run and must match.
- **After-numbers, three measured states** (all from this worktree after the edit):
  1. Evidence files absent, before commit/push: green=1 `['AC-8']`, red=7
     `['AC-1', 'AC-2', 'AC-3', 'AC-4', 'AC-5', 'AC-6', 'AC-7']`, zero silent blocks. AC-1/2/3/6 stay
     red (SW's work), AC-4/AC-5 print their new-path guards
     (`FAIL AC-4: _docs/baselines/T7-main-pre-swap.py not found ...`,
     `FAIL AC-5: CONTRIBUTING.md Constraints Convention incomplete ...`), and AC-7 is red only on its
     push arm for the reason in its note above.
     This is the detached/pinned-tree view and it is not the baseline this groom claims. The
     round-1 baseline (green=2 `['AC-7', 'AC-8']`, red=6 `['AC-1'..'AC-6']`) was measured on a tree
     whose `HEAD` equalled `origin/HEAD`, and the only line that differs here is AC-7's push arm, so
     the comparable before/after pair for this repair is green=2 / red=6 on a pushed tree in both
     rounds: the repair turned no AC green and turned no AC red.
  2. Throwaway evidence + contract-visible throwaway implementation (byte-exact
     `_docs/baselines/T7-main-pre-swap.py` from `git show main:backend/app/main.py`, one
     `_docs/examples/*.md`, the U+2011/U+2013 swapped for ASCII hyphens in `backend/app/main.py`, a
     `## Constraints Convention` section in `CONTRIBUTING.md`): green=3 `['AC-4', 'AC-5', 'AC-8']`,
     red=5 `['AC-1', 'AC-2', 'AC-3', 'AC-6', 'AC-7']`. Both repaired ACs print their PASS lines, so
     neither is green-unreachable; AC-8's PASS proves the new allowed-path set accepts the new
     evidence paths, and AC-7's FAIL carried only the push arm even with two extra example/baseline
     files and an edited `CONTRIBUTING.md`/`main.py` present - the moved CJK/CR globs really do read
     them.
  3. Partial-evidence negative control: with the byte-exact snapshot present but the character swap
     undone, AC-4 prints `FAIL AC-4: backend/app/main.py still carries 9 non-ASCII codepoint(s):
     U+2011 x8, U+2013 x1` - the red side of its own two-sided guard, so the snapshot alone cannot
     buy a false green.
  Every throwaway was then deleted and `CONTRIBUTING.md` / `backend/app/main.py` restored with
  `git checkout --` (verified by md5sum against the pre-probe copies), leaving
  `git status --porcelain` = ` M _docs/issues/T7.md` plus the untracked brief only. The honest
  baseline this issue is dispatched against is therefore state 1 plus AC-7's push arm, which turns
  green with the worker's own push: `main.py` still carries 9 non-ASCII codepoints on `origin/main`.

## Test requirements

SW fills this in. Nothing in this issue changes product behaviour, so no new pytest module is
expected; the verification surface is the eight AC blocks above, which are the tests.

## Implementation notes

SW fills this in. One constraint worth stating before SW picks it up: AC-4's character swap must be
comment-and-docstring-only, and the proof is the pre-swap baseline file AC-4's block compares
against, not a code-diff argument.

## Dependencies

- Blocked by: **a discovery, not a ticket** - `_docs/debt-register.md` does not exist on `main`.
  AC-1 brings that file into this issue's write set (Constraints below) precisely so T7 can land it
  itself; if the orchestrator would rather merge branch `docs/PM-debt-register` first, that merge
  must happen before this issue's AC-1/AC-6 can go green, and the ACs say so in red rather than
  passing vacuously. Nothing else blocks this issue: `depends: []` in the front matter is unchanged.
- Round-2 reachability: both repaired ACs are green-reachable and red-honest. See the three
  measured states in Round notes above - AC-4 and AC-5 print their PASS lines with a throwaway
  tracked-path snapshot plus a throwaway `## Constraints Convention` section, AC-4 prints its
  non-ASCII FAIL line when only the snapshot is present, and both print new-path FAIL guards when
  the evidence is absent. Nothing outside AC-4/AC-5/AC-7/AC-8 moved: AC-1, AC-2, AC-3 and AC-6 print
  byte-identical FAIL lines before and after this repair.
- Blocks: nothing in `_docs/backlog.md`; the register's Part 3 queue lists T6 / T9 / T11 as
  siblings, not downstream work.
- Touches-but-does-not-own: `_docs/testing.md` was being edited on `issue/B-17-test-isolation`
  (Platform #58) when this line was written; that branch has since merged (`36b80f4`). AC-3 is
  written as a run-time invariant for exactly that reason, and this issue's write access to that file
  is bounded to the `test_*.py` filename entries inside its `### Structure` code block (Constraints,
  round-3 narrow exception) so that a later B-17-series section addition and this issue's list repair
  cannot collide outside that block.

## Out of scope

- Editing any merged issue file: `_docs/issues/B-08.md`, `_docs/issues/B-10.md`,
  `_docs/issues/B-11.md`, `_docs/issues/B-12.md`, `_docs/issues/B-17.md` and every P0-*/D-*/F-*
  file. AC-2's target file `_docs/issues/B-11.md` is the single exception the debt register
  authorises, and only for the three paperwork lines AC-2 names.
- Product behaviour, endpoints, schemas, service logic. AC-4 touches `backend/app/main.py` for
  comment characters and nothing else.
- The B-09 / T9 sibling-collision AC re-wording (register ticket T9), the B-07 mirror restore
  (register ticket T11), and the gate-tool md5 pinning (register ticket T8).
- The D1 re-judge verdict itself: AC-6 requires the judgement to be recorded with a commit SHA,
  and the judgement call belongs to the register row, not to this round's code (round 3 re-measured:
  B-17's fixtures are on `main` since `36b80f4`, so the judgement is now *performable* - it is still
  out of scope here because a groom does not write the verdict its own AC only checks for).
- `_docs/issue-map.json`: read by AC-8, never written by this issue.
- `backend/tests/conftest.py` and every `backend/tests/*.py` file: owned by B-17 #58 while it is in
  flight. AC-2 removes B-11's claim on nonexistent fixtures instead of adding the fixtures, because
  adding them would collide with B-17.
- Any Platform label change, including `groomed`. Label application belongs to the Orchestrator.

## Constraints

Files this issue may add or modify, and nothing else. **Round-3 note for the reader:** this list and
AC-8's allowed-path regex are now the same set, kept in sync deliberately - the round-2 version of
this issue failed because the two drifted apart, which is exactly the D9 defect this issue exists to
legislate (see AC-5 and AC-8's round-3 `Measured today`). If a future round adds a write target to
one of these two places, it must be added to both:

- `_docs/issues/T7.md` (this file, groomed body).
- `_docs/debt-register.md` - **new on main; reason:** AC-1 and AC-6 cannot be satisfied on a tree
  where the ledger they complete does not exist, and the register's own Part 1 says the ledger must
  not live in a private notebook. Owning it is scope item 1 of Platform #59.
- `_docs/issues/B-11.md` - **exception with a narrow reason:** scope item 2 is B-11 paperwork. The
  only lines implementation may change are the Metadata `Owner stub` count, the Definition of Done
  first-line count, and the Test requirements bullet that names fixtures. No AC text, no DoD
  semantics beyond the count.
- `CONTRIBUTING.md` (scope item 4: the `## Constraints Convention` section AC-5 checks).
- `backend/app/main.py` (scope item 5: comment/docstring characters only, zero code change, the
  invariant AC-4 checks).
- `_docs/examples/` - **new directory; reason:** AC-5 requires the convention to point at a
  worked example, and that example is a checked-in document, so it lives at a TRACKED path.
  Round 1 named `_docs/state/examples/`, which `.gitignore` line 88 (`_docs/state/`) ignores
  wholesale, so nothing there can ever be committed (measured: `git check-ignore -v
  _docs/state/examples/T7-worked-example.md` -> `.gitignore:88:_docs/state/`; and
  `git check-ignore -v _docs/examples/T7-worked-example.md` -> exit 1, not ignored).
- `_docs/baselines/T7-main-pre-swap.py` - **new file; reason:** AC-4's "zero code change"
  claim is only mechanical if a pre-swap snapshot exists to compare against, and that snapshot is
  evidence the gate must be able to read, so it must be tracked. Round 1 named
  `_docs/state/baselines/T7-main-pre-swap.py`; `git check-ignore -v` on it prints
  `.gitignore:88:_docs/state/`, i.e. it could never enter the diff AC-8 audits.
- `_docs/log/` if this repo's convention requires a log line per change.

Deliberately **not** in the list, each because another party owns it or a frozen gate reads it:
`_docs/testing.md` (B-17 #58 is editing it, and AC-3 is shaped to survive that),
`_docs/issues/B-08.md` / `B-10.md` / `B-12.md` / `B-17.md` (the `runacs ... --only AC-14` gates
scan their own file), `backend/tests/` (B-17 #58), `_docs/issue-map.json` (orchestrator
bookkeeping), `.tq-orchestrator/` (harness; children never edit it, and no AC here calls it).

Guidelines to follow: repo content is English-only, zero CJK and zero CR bytes in every file this
issue touches; `uv run pytest` / `ruff` must stay clean on anything this issue edits; the mirror
budget of 200,000 bytes is a hard deliverable limit; and a debt note found in a file this issue may
not edit is recorded as follow-up prose in the register rather than fixed here.

## Definition of Done

- [ ] All 8 AC blocks print their PASS line from the repo root on this worktree (issue-specific:
  AC-1/AC-2/AC-3/AC-4/AC-5/AC-6 are red on `main` today by design and must be turned green by this
  issue's edits; AC-7 and AC-8 are hygiene the groom already satisfies and are stated rather than
  faked red).
- [ ] All acceptance criteria pass.
- [ ] Backend tests pass, and the run is reported as a delta against the pre-branch baseline rather
  than an absolute "0 failed" claim (the suite-wide absolute belongs to B-17 #58).
- [ ] Frontend tests pass (not applicable: this issue touches no frontend file).
- [ ] No lint errors: `ruff` prints `All checks passed!` on `backend/app/main.py`.
- [ ] Gate from Platform #59 verified: `runacs B-08 --only AC-14` and `runacs B-11 --only AC-14`
  green, CJK/CR scan 0, `ruff` clean, `git ls-remote origin HEAD` equals the local head.
- [ ] Mirror verified: `_docs/issues/T7.md` mirrored to Platform #59 and the byte counts agree.
- [ ] `_docs/debt-register.md` rows D5, D7, D9 and D1 updated in the same commit that closes them,
  so no debt is closed silently.
- [ ] Manual verification completed: a reviewer reads the register's T7 row and can trace each of
  the five owner-approved items to an AC in this file.
- [ ] Review verdict recorded, then merged by the Orchestrator.
