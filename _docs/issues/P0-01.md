---
id: P0-01
title: Write _docs/specs.md
depends: []
platform_issue: 1
---

<!-- PM: fill remaining sections -->

## Metadata

## Goal
Produce the full specification for TableQueue covering roles, flows, state machine, data model, business rules, error codes, API summary, time/timezone, concurrency, security, known limitations, and non-goals.

## Context

## Acceptance criteria
- [ ] `_docs/specs.md` exists and remains the authoritative source for its scope; it is not replaced by a new file.
- [ ] All sections required of `_docs/specs.md` by `_docs/plan.md` are present: 21 sections: Overview, Goals and Non-Goals, Roles and Permissions, Core Flows, State Machine, Data Model, Business Rules, Queue Number and Business Date, Table Management, Notifications, Error Codes, API Summary, Time and Timezone, Concurrency, Security and Privacy, Known Limitations, Non-Goals, Environment Variables, Seed Data, Demo Script, Definition of Done.
- [ ] All decisions from Phase 0 Q&A are captured
- [ ] Date format `2026-09-10 21:00`; phone format Taiwan
- [ ] The API summary in section 12 lists exactly the operations defined in `_docs/openapi.yaml` paths; any operation present in one and missing from the other is reconciled.
- [ ] The error codes in section 11 match the `error.code` values used by `components.responses` in `_docs/openapi.yaml`.
- [ ] The status colour values referenced by `_docs/ui.md` section 2 do not contradict `_docs/specs.md`.
- [ ] `_docs/specs.md` is 100% English: run `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/specs.md` and confirm it returns `0` (100% English, no CJK characters)
- [ ] Every inconsistency surfaced by the checks above is corrected in this issue's branch, or recorded as an explicit follow-up issue if it belongs to another document's scope.
- [ ] No application code is created or modified by this issue.

## Test requirements

## Implementation notes

## Dependencies

## Out of scope

## Constraints
- File: `_docs/specs.md`
- Do not modify other files
- Audit scope: only `_docs/specs.md` may change, and only to fix an inconsistency this issue's acceptance criteria identified
- English only: no CJK characters in any file this issue touches

## Definition of Done

