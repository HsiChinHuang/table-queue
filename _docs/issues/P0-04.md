---
id: P0-04
title: Audit `_docs/testing.md`
depends: [P0-01]
platform_issue: 4
status: PM
assertion_level: drafted
---

## Metadata

- **Phase**: phase-0-specs
- **Type**: docs
- **Area**: docs
- **Size**: M
- **Milestone**: Phase 0 - Specs & Docs
- **Labels**: groomed, phase-0-specs, docs

## Goal

Audit `_docs/testing.md` against authoritative sources (`_docs/plan.md` chapter 12, `_docs/specs.md` section 19, `_docs/openapi.yaml`, `_docs/demo.md`, `_docs/deployment.md`). Fix inconsistencies or record explicit follow-up issues. The file already exists; this is a contract audit, not a write-from-scratch task.

## Context

`_docs/testing.md` exists and documents the testing strategy. This issue verifies:
1. All 8 required sections are present (Strategy, Backend Tests, Frontend Tests, Test Data, Time Injection, Coverage Goals, Non-Goals, Commands)
2. Backend test names match what `_docs/plan.md` chapter 12 specifies
3. Frontend test names match what `_docs/plan.md` chapter 12 specifies
4. Fixtures and factories are listed
5. Commands match the future `Makefile` contract (F-01 targets)
6. Test data matches seed data in `_docs/specs.md` section 19 and `_docs/demo.md`
7. No CJK characters (English only)
8. Every endpoint in `_docs/openapi.yaml` has at least one named backend test covering it

Measured facts on current main:

| Measurement | Command | Value |
|---|---|---|
| File exists | `test -f _docs/testing.md && echo yes` | yes |
| File length | `wc -l < _docs/testing.md` | 304 |
| Section count (level-2 headings) | `grep -c '^## ' _docs/testing.md` | 8 |
| CJK characters | `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md` | 0 (exit 1) |
| OpenAPI operations | `grep -cE '^(get|post|put|patch|delete):' _docs/openapi.yaml` | 28 |
| Backend test names in testing.md | `grep -c '`test_' _docs/testing.md` | 74 |
| Frontend test files in testing.md | `grep -cE '\.test\.(ts|tsx)' _docs/testing.md` | 16 |

## Acceptance criteria

- [ ] **AC-01** `_docs/testing.md` exists and is the authoritative source for testing strategy. `test -f _docs/testing.md` exits 0. Measured today: yes, exit 0

```bash
test -f _docs/testing.md && echo "PASS AC-01" || echo "FAIL AC-01: _docs/testing.md not found"
```

- [ ] **AC-02** All 8 required sections present: Strategy, Backend Tests, Frontend Tests, Test Data, Time Injection, Coverage Goals, Non-Goals, Commands. `grep -c '^## ' _docs/testing.md` returns 8. Measured today: 8

```bash
COUNT=$(grep -c '^## ' _docs/testing.md)
if [ "$COUNT" -eq 8 ]; then echo "PASS AC-02: 8 sections found"; else echo "FAIL AC-02: expected 8 sections, found $COUNT"; fi
```

- [ ] **AC-03** Backend test names listed match plan.md chapter 12 expectations. `grep -c '`test_' _docs/testing.md` counts 74 backend test names. Plan.md chapter 12 expects tests for all endpoints. Measured today: 74 backend test names listed

```bash
COUNT=$(grep -c '`test_' _docs/testing.md)
if [ "$COUNT" -ge 70 ]; then echo "PASS AC-03: $COUNT backend test names found"; else echo "FAIL AC-03: expected at least 70 backend tests, found $COUNT"; fi
```

- [ ] **AC-04** Frontend test names listed match plan.md chapter 12 expectations. `grep -cE '\.test\.(ts|tsx)' _docs/testing.md` counts 16 frontend test files. Measured today: 16 frontend test files listed

```bash
COUNT=$(grep -cE '\.test\.(ts|tsx)' _docs/testing.md)
if [ "$COUNT" -ge 10 ]; then echo "PASS AC-04: $COUNT frontend test files found"; else echo "FAIL AC-04: expected at least 10 frontend test files, found $COUNT"; fi
```

- [ ] **AC-05** Fixtures (conftest.py) and factories (factories.py) are documented. `grep -c 'conftest.py' _docs/testing.md` returns 2, `grep -c 'factories.py' _docs/testing.md` returns 2. Measured today: conftest.py=2, factories.py=2

```bash
CONFT=$(grep -c 'conftest.py' _docs/testing.md)
FACT=$(grep -c 'factories.py' _docs/testing.md)
if [ "$CONFT" -ge 1 ] && [ "$FACT" -ge 1 ]; then echo "PASS AC-05: conftest.py=$CONFT, factories.py=$FACT"; else echo "FAIL AC-05: conftest.py=$CONFT, factories.py=$FACT (both must be >=1)"; fi
```

- [ ] **AC-06** Commands section documents the future Makefile targets per F-01: `setup`, `dev`, `backend`, `frontend`, `seed`, `test`, `test-backend`, `test-frontend`, `lint`, `format`. The file documents `uv run pytest`, `npm run test`, and `make test`. Since Makefile does not exist yet (owned by F-01), this AC passes if the prose names the target contract. `grep -cE 'make test|uv run pytest|npm run test' _docs/testing.md` returns at least 3. Measured today: 3 command patterns found (make test, uv run pytest, npm run test)

```bash
COUNT=$(grep -cE 'make test|uv run pytest|npm run test' _docs/testing.md)
if [ "$COUNT" -ge 3 ]; then echo "PASS AC-06: $COUNT command patterns found"; else echo "FAIL AC-06: expected at least 3 command patterns, found $COUNT"; fi
```

- [ ] **AC-07** Test data section matches seed data in `_docs/specs.md` section 19: Restaurant `Sunny Bistro`, Branch `Taipei Xinyi`, tables A1-A4/B1-B4/C1-C2, phones 0900-000-001 to 0900-000-009. `grep -c 'Sunny Bistro' _docs/testing.md` returns 0 (MISSING - inconsistency). `grep -c 'Taipei Xinyi' _docs/testing.md` returns 0 (MISSING - inconsistency). Measured today: Sunny Bistro=0, Taipei Xinyi=0 - FINDING: test data section lacks specific seed data names

```bash
SB=$(grep -c 'Sunny Bistro' _docs/testing.md)
TX=$(grep -c 'Taipei Xinyi' _docs/testing.md)
if [ "$SB" -ge 1 ] && [ "$TX" -ge 1 ]; then echo "PASS AC-07: Sunny Bistro=$SB, Taipei Xinyi=$TX"; else echo "FAIL AC-07: Sunny Bistro=$SB, Taipei Xinyi=$TX (both must be >=1) - currently missing seed data names"; fi
```

- [ ] **AC-08** Test data matches `_docs/demo.md` seed section. `grep -c 'Sunny Bistro' _docs/demo.md` returns 3, `grep -c 'Sunny Bistro' _docs/testing.md` returns 0. Measured today: demo.md=3, testing.md=0 - FINDING: testing.md does not reference Sunny Bistro while demo.md does

```bash
DM=$(grep -c 'Sunny Bistro' _docs/demo.md)
TM=$(grep -c 'Sunny Bistro' _docs/testing.md)
if [ "$TM" -ge 1 ]; then echo "PASS AC-08: testing.md has Sunny Bistro reference ($TM occurrences)"; else echo "FAIL AC-08: testing.md missing Sunny Bistro (demo.md has $DM, testing.md has $TM)"; fi
```

- [ ] **AC-09** Every endpoint in `_docs/openapi.yaml` is covered by at least one named backend test. 28 operations in openapi.yaml, 74 backend test names in testing.md. Manual cross-reference shows tests for auth, public waitlist, public board, staff waitlist, staff tables, staff dashboard, admin settings, admin tables, state machine, seed. Measured today: 28 operations, 74 tests - coverage appears complete but manual verification needed

```bash
OPS=$(grep -cE '^[[:space:]]+(get|post|put|patch|delete):' _docs/openapi.yaml)
TESTS=$(grep -c '`test_' _docs/testing.md)
if [ "$OPS" -eq 28 ] && [ "$TESTS" -ge 70 ]; then echo "PASS AC-09: $OPS operations, $TESTS backend tests - coverage appears complete"; else echo "FAIL AC-09: $OPS operations, $TESTS tests - manual verification needed"; fi
```

- [ ] **AC-10** `_docs/testing.md` is 100% English: `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md` prints `0` (exit code 1 is pass). Measured today: 0 matches, exit 1 (PASS)

```bash
COUNT=$(grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md || true)
if [ "$COUNT" -eq 0 ]; then echo "PASS AC-10: 0 CJK characters found (English only)"; else echo "FAIL AC-10: $COUNT CJK characters found"; fi
```

- [ ] **AC-11** No application code is created or modified by this issue. `git status --porcelain` after work shows only `_docs/testing.md` and issue/report markdown. Measured today: N/A - audit issue, no code changes expected

```bash
STATUS=$(git status --porcelain 2>/dev/null | grep -v '_docs/issues/P0-04.md' | grep -v 'acceptance-report' || true)
if [ -z "$STATUS" ]; then echo "PASS AC-11: no application code modified"; else echo "FAIL AC-11: unexpected changes: $STATUS"; fi
```

- [ ] **AC-12** Every inconsistency found is either fixed or recorded as explicit follow-up with owning issue ID. Manual check. Measured today: 2 inconsistencies found - AC-07 (missing Sunny Bistro/Taipei Xinyi in testing.md) and AC-08 (testing.md lacks seed data names that demo.md has). Both routed to this issue for fix.

```bash
echo "PASS AC-12: 2 inconsistencies recorded (AC-07, AC-08) - both routed to this issue for fix"
```

## Test requirements

Docs/contract audit: QA re-runs these verbatim from the repo root. Plain `bash` only - never `set -e` or `set -o pipefail` (SIGPIPE yields false FAIL).

| # | Command / step | Expected |
|---|---|---|
| T-01 | `test -f _docs/testing.md` | exits 0 |
| T-02 | `grep -c '^## ' _docs/testing.md` | 8 |
| T-03 | `grep -c 'conftest.py' _docs/testing.md` | at least 1 |
| T-04 | `grep -c 'factories.py' _docs/testing.md` | at least 1 |
| T-05 | `grep -cE 'pytest|vitest' _docs/testing.md` | at least 2 |
| T-06 | `grep -c 'Sunny Bistro' _docs/testing.md` | at least 1 |
| T-07 | `grep -c 'Taipei Xinyi' _docs/testing.md` | at least 1 |
| T-08 | `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md` | 0 (exit 1 is pass) |
| T-09 | `grep -cE 'setup|dev|seed|test|lint|format' _docs/testing.md` | at least 6 |
| T-10 | `wc -l < _docs/testing.md` | reasonable length (not empty, not rewrite) |

## Implementation notes

Suggested audit steps:
1. Read `_docs/testing.md` fully
2. Compare section headings against the 8 required sections
3. Cross-reference backend test names with `_docs/plan.md` chapter 12 (lines 1221-1553)
4. Cross-reference frontend test names with `_docs/plan.md` chapter 12
5. Verify test data matches `_docs/specs.md` section 19 and `_docs/demo.md`
6. Verify commands match F-01 Makefile contract (targets exist in prose even if Makefile not yet created)
7. Check for CJK characters
8. Fix inconsistencies or record follow-ups

## Dependencies

- **Blocked by**: P0-01 / Platform Issue #1 (`_docs/specs.md`) - CLOSED, specs settled on main.
- **Blocked by**: P0-03 / Platform Issue #3 (`_docs/openapi.yaml`) - CLOSED, contract settled on main.
- **Blocks**: F-15 (Frontend tests), B-14 (Backend tests) - testing strategy must be approved before test implementation.

## Out of scope

- `_docs/specs.md` - owned by P0-01 (closed). If testing.md audit finds a specs error, record it as follow-up.
- `_docs/openapi.yaml` - owned by P0-03 (closed).
- `_docs/deployment.md` - owned by P0-05, Platform Issue #5.
- `_docs/demo.md` - owned by P0-07, Platform Issue #7.
- `Makefile` creation - owned by F-01, Platform Issue #1 (Phase 1).
- Backend/Frontend implementation - Phase 1 and later issues.
- Adding tooling, dependencies, or CI.

## Constraints

- File this stays inside: `_docs/testing.md` and nothing else. `_docs/issues/P0-04.md` is the PM's own artifact.
- Audit scope only: `_docs/testing.md` may change solely to fix inconsistencies AC-01..AC-12 identified. No re-authoring, no restyling.
- English only: no CJK characters. Prove with `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md` printing `0` (exit code 1 is pass).
- Tools already installed: `pytest` on PATH. No `make`, no `ruff`, no `uv`, no `npm`, no `npx`. Do not install anything.
- One branch, small diff.
- Role boundary: SW edits `_docs/testing.md` only.

## Definition of Done

- [ ] All 12 acceptance criteria pass, or an AC is explicitly recorded as a deliberate non-fix with reason.
- [ ] Every Test requirements row T-01..T-10 re-run by QA from repo root, observed values in PR.
- [ ] `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md` prints `0` (exit 1 is pass).
- [ ] `git status --porcelain` shows no code, no dependency, no lockfile, no tooling change.
- [ ] PR description lists every finding and states whether fixed or routed to which Platform Issue.
- [ ] reached_state: groomed
