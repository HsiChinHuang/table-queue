---
id: P0-04
title: Write _docs/testing.md
depends: [P0-01]
platform_issue: 4
---

<!-- PM: fill remaining sections -->

## Metadata

## Goal
Produce the testing strategy, test names, fixtures, time injection, coverage goals, non-goals, and commands.

## Context

## Acceptance criteria
- [ ] `_docs/testing.md` exists and remains the authoritative source for its scope; it is not replaced by a new file.
- [ ] All sections required of `_docs/testing.md` by `_docs/plan.md` are present: 8 sections: Strategy, Backend Tests, Frontend Tests, Test Data, Time Injection, Coverage Goals, Non-Goals, Commands.
- [ ] All backend test names listed
- [ ] All frontend test names listed
- [ ] Fixtures and factories listed
- [ ] Commands documented
- [ ] Every endpoint in `_docs/openapi.yaml` is covered by at least one named backend test.
- [ ] The commands in section 8 match the `Makefile` targets required by `_docs/plan.md` F-01 and the commands in `_docs/deployment.md`.
- [ ] Test data in section 4 matches the seed data in `_docs/specs.md` section 19 and `_docs/demo.md`.
- [ ] `_docs/testing.md` is 100% English: run `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/testing.md` and confirm it returns `0` (100% English, no CJK characters)
- [ ] Every inconsistency surfaced by the checks above is corrected in this issue's branch, or recorded as an explicit follow-up issue if it belongs to another document's scope.
- [ ] No application code is created or modified by this issue.

## Test requirements

## Implementation notes

## Dependencies

## Out of scope

## Constraints
- File: `_docs/testing.md`
- Audit scope: only `_docs/testing.md` may change, and only to fix an inconsistency this issue's acceptance criteria identified
- English only: no CJK characters in any file this issue touches

## Definition of Done

