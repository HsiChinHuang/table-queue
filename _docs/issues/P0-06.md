---
id: P0-06
title: Write _docs/api-examples.md
depends: [P0-03]
platform_issue: 6
---

<!-- PM: fill remaining sections -->

## Metadata

## Goal
Produce curl examples for every endpoint.

## Context

## Acceptance criteria
- [ ] `_docs/api-examples.md` exists and remains the authoritative source for its scope; it is not replaced by a new file.
- [ ] All sections required of `_docs/api-examples.md` by `_docs/plan.md` are present: one example block per API group: Public, Auth, Staff waitlist, Staff tables, Staff dashboard, Admin settings, Admin tables, Admin reset, Health, plus an Error responses section.
- [ ] Success and error examples included
- [ ] Uses `$TOKEN`, `$ID`, `$TABLE_ID` placeholders
- [ ] Assumes backend at `http://localhost:8000`, branch `1`, PIN `1234`
- [ ] Every one of the 28 operations in `_docs/openapi.yaml` `paths` has a matching curl example, and no example targets an operation absent from `_docs/openapi.yaml`.
- [ ] Every error code in `_docs/specs.md` section 11 that is reachable over HTTP has a response example, and each example body uses the `error.code` / `error.message` / `error.details` shape from that section.
- [ ] Request bodies match the corresponding `components.schemas` in `_docs/openapi.yaml`.
- [ ] The file's fenced code blocks are balanced (an even number of ``` fences) so it renders correctly.
- [ ] `_docs/api-examples.md` is 100% English: run `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/api-examples.md` and confirm it returns `0` (100% English, no CJK characters)
- [ ] Every inconsistency surfaced by the checks above is corrected in this issue's branch, or recorded as an explicit follow-up issue if it belongs to another document's scope.
- [ ] No application code is created or modified by this issue.

## Test requirements

## Implementation notes

## Dependencies

## Out of scope

## Constraints
- File: `_docs/api-examples.md`
- Audit scope: only `_docs/api-examples.md` may change, and only to fix an inconsistency this issue's acceptance criteria identified
- English only: no CJK characters in any file this issue touches

## Definition of Done

