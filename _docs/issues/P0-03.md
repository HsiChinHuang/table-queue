---
id: P0-03
title: Write _docs/openapi.yaml
depends: [P0-01]
platform_issue: 3
---

<!-- PM: fill remaining sections -->

## Metadata

## Goal
Produce the OpenAPI 3.0.3 contract for all endpoints.

## Context

## Acceptance criteria
- [ ] `_docs/openapi.yaml` exists and remains the authoritative source for its scope; it is not replaced by a new file.
- [ ] All sections required of `_docs/openapi.yaml` by `_docs/plan.md` are present: openapi, info, servers, tags, paths and components (parameters, responses, securitySchemes, schemas).
- [ ] `openapi: 3.0.3`
- [ ] `info.title: TableQueue API`, `info.version: 0.1.0`
- [ ] `servers` points at `http://localhost:8000`
- [ ] Tags: `public`, `auth`, `staff`, `admin`, `health`
- [ ] `components.parameters`: `BranchId`, `QueueNumber`, `Id`
- [ ] `components.responses`: `ValidationError`, `Unauthorized`, `WaitlistNotFound`, `WaitlistInvalidStatus`, `TableNotFound`, `BranchNotFound`, `RateLimited`
- [ ] `components.securitySchemes.bearerAuth`
- [ ] Examples for every endpoint and error code
- [ ] Examples match seed data
- [ ] Every operation in `_docs/specs.md` section 12 (API Summary) has a matching path and method in `paths`, and every `paths` entry appears in that summary; the two lists reconcile exactly.
- [ ] All 29 `components.schemas` entries are present and referenced: BoardResponse, CancelWaitlistRequest, CancelledReason, ChangePinRequest, CreateTableRequest, DashboardResponse, EditWaitlistRequest, ErrorResponse, HealthResponse, JoinWaitlistRequest, PublicBranchResponse, ReorderWaitlistRequest, ResetDataRequest, SeatWaitlistRequest, SettingsResponse, StaffLoginRequest, StaffLoginResponse, TableListResponse, TableResponse, TableStatus, UpdateSettingsRequest, UpdateTableRequest, UpdateTableStatusRequest, WaitlistEntryResponse, WaitlistFilterStatus, WaitlistListResponse, WaitlistSource, WaitlistStatus, WaitlistStatusResponse.
- [ ] Enum values in `WaitlistStatus`, `TableStatus`, `CancelledReason`, `WaitlistSource` and `WaitlistFilterStatus` match `_docs/specs.md` sections 5 and 7.
- [ ] The file parses as YAML and passes an OpenAPI 3.0.3 lint with no errors.
- [ ] `_docs/openapi.yaml` is 100% English: run `grep -cP '[\x{4E00}-\x{9FFF}]' _docs/openapi.yaml` and confirm it returns `0` (100% English, no CJK characters)
- [ ] Every inconsistency surfaced by the checks above is corrected in this issue's branch, or recorded as an explicit follow-up issue if it belongs to another document's scope.
- [ ] No application code is created or modified by this issue.

## Test requirements

## Implementation notes

## Dependencies

## Out of scope

## Constraints
- File: `_docs/openapi.yaml`
- Audit scope: only `_docs/openapi.yaml` may change, and only to fix an inconsistency this issue's acceptance criteria identified
- English only: no CJK characters in any file this issue touches

## Definition of Done

