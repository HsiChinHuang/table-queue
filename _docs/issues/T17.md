---
id: T17
title: Frontend client lifecycle: expires_at honoured, router-driven redirect, shared-UI resource cleanup
depends:[]
platform_issue: 72
---

## Metadata

- **Phase**: phase-5-audit
- **Type**: security-audit
- **Area**: audit-batch
- **Size**: M
- **Milestone**: Security audit remediation (operator directive 2026-09-13)
- **Priority**: normal

## Reason (audit provenance)

SEC AUDIT MEDIUM (C-4 C-7 C-9, C-3 redirect half): expires_at delivered-but-never-consumed (expired token keeps UI mounted + replays); AudioContext per beep never closed (accumulates to Chrome cap = audio dies mid-shift); untracked post-unmount timer. Severity medium.

## Acceptance

client stores expiresAt with the token and treats expiry as logout (hydrated guard + timer -> destroySession; request() refuses to send a known-expired bearer); 401 redirect uses ROUTES constant + location.assign with already-on-login guard; ONE shared 1Hz ticker replaces per-card useCountdown intervals (WaitlistCard fan-out preserved); SoundToggle holds a single lazily-created AudioContext reused across beeps and closed on teardown, first-interaction unlock registered once; ConnectionBanner.resetFailure setTimeout tracked+cleared

## Files

frontend/src/api/client.ts frontend/src/hooks/useCountdown.ts frontend/src/components/WaitlistCard.tsx frontend/src/components/SoundToggle.tsx frontend/src/components/ConnectionBanner.tsx frontend/src/layouts/StaffLayout.tsx frontend/src/**/tests
