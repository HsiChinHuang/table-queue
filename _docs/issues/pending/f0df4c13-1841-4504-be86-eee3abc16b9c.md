title: Suite-order poisoning: test_middleware.py::test_cors_allowlist reloads app.main and leaves router limiter globals bound to a stale Limiter (B-06 AC-14 identity + 429 arms red in full-suite runs only)
acceptance: `pytest -q -p no:randomly` (bare, whole suite, and pinned) reports 0 failed on the pair
  tests/test_public_waitlist.py::test_public_limiter_is_the_apps_limiter and
  tests/test_public_waitlist.py::test_status_lookup_limit_is_ten_per_minute WITHOUT changing either
  assertion and without editing B-06's files; the fix lives in tests/test_middleware.py (restore the
  module-level limiter identity after importlib.reload, or stop reloading app.main) or in a shared
  restore seam; two-sided proof: the pair is green after the fix, and re-inserting the old reload
  turns it red again. Order-dependent repro must be shown both directions.
files: backend/tests/test_middleware.py, backend/tests/test_public_waitlist.py (read-only evidence: assertions unchanged)
depends: [B-17]
reason: "Parent-measured 2026-09-12 at the B-17 tip 313a25f in an independent gate worktree. After
  B-17's isolation fix the whole-suite run goes 27 failed -> 2 failed, and the 2 survivors are
  B-06's limiter-identity + 429-observable tests. Mechanism measured: test_cors_allowlist does
  get_settings.cache_clear(); importlib.reload(app.main), which builds a NEW Limiter and a NEW
  app.state.limiter; app.routers.public keeps its module global `limiter` pointing at the OLD object
  (it is injected only via configure_limiter at first import), so after the reload
  `public.limiter is app.state.limiter` is False and the 429 arm prices against a limiter the app no
  longer owns. Repro both directions: `pytest -q -p no:randomly tests/test_middleware.py
  tests/test_public_waitlist.py::test_public_limiter_is_the_apps_limiter` -> 1 failed; the same two
  nodes in the reverse order -> 5 passed. Whole-suite minus test_middleware.py -> 128 passed, 0
  failed (scratch run, not a commit). Same reload also poisons closed-issue ACs that run the whole
  suite: B-05 AC-15 (red 2 failed at the B-17 tip; red 27 failed on main) and B-04 AC-16 (red at the
  B-17 tip AND red on main, same 2/27 survivors). B-06 itself = 15/15 green at the B-17 tip. This is
  why B-17 AC-2/AC-3 stay red at close and why its PR must name this issue."
priority: high
