---
id: T30
title: Frontend session storage: one store, one key, remember-me actually controls persistence, real logout
depends:[]
platform_issue: 85
---

## Metadata

- **Phase**: phase-5-audit
- **Type**: security-audit
- **Area**: audit-batch
- **Size**: M
- **Milestone**: Security audit remediation (operator directive 2026-09-13)
- **Priority**: high

## Reason (audit provenance)

SEC AUDIT HIGH (C-1 C-2 C-3): remember-me OFF still lands TWO durable localStorage copies (jsdom-proven); removeItem count in product code = 0 so logout never clears the sessionStorage copy - live JWT readable on shared front-desk hardware after logout; 401 clears 1 of 3 copies; two stores disagree so the app can be logged-out and authenticated at once. Severity HIGH.

## Acceptance

single persisted store owns the staff token (raw LoginPage setItem removed; duplicate stores/staffStore token field removed; no bare-JWT vs envelope key collision); remember=false => sessionStorage ONLY (jsdom test proves localStorage empty after OFF login); destroySession() clears every slot both storages + both legacy keys and is called by logout button AND 401 handler; regression test asserts all four slots empty post-logout and post-401; startup migration purges legacy keys

## Files

frontend/src/pages/staff/LoginPage.tsx frontend/src/api/staffStore.ts frontend/src/stores/staffStore.ts frontend/src/api/client.ts frontend/src/layouts/StaffLayout.tsx frontend/src/**/tests
