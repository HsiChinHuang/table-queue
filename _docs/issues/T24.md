---
id: T24
title: SQLite-only reality vs PostgreSQL affordance: add the driver and exercise the path, or remove the claim
depends:[]
platform_issue: 79
---

## Metadata

- **Phase**: phase-5-audit
- **Type**: security-audit
- **Area**: audit-batch
- **Size**: M
- **Milestone**: Security audit remediation (operator directive 2026-09-13)
- **Priority**: normal

## Reason (audit provenance)

SEC AUDIT INFO/portability (B-10): PG URL fails at import (psycopg2 absent) - clean failure today, but docs/config still advertise the branch; connect_args timeout is sqlite-flavoured. Every write-integrity claim in the audit is SQLite-scoped BECAUSE of this. Needs an operator/product decision - filed so it is decided, not drifted. Severity low/info.

## Acceptance

DECISION issue - option A: psycopg2-binary added to requirements/lock, PG boot + one-write smoke test green in CI-postgres job (depends CI issue), connect_args made dialect-aware (timeout is sqlite-only - currently a portable-API misuse per B-10); option B: docs/config stop claiming PG support and the code refuses non-sqlite URLs with a clear startup error. Either way a fresh clone cannot believe a PG promise that cannot boot

## Files

backend/app/database.py backend/requirements.txt README.md deployment docs backend/tests/
