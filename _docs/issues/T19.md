---
id: T19
title: Store contention is a contract outcome: busy/locked maps to a retryable code, WAL or documented single-writer, no silent 500
depends:[]
platform_issue: 74
---

## Metadata

- **Phase**: phase-5-audit
- **Type**: security-audit
- **Area**: audit-batch
- **Size**: M
- **Milestone**: Security audit remediation (operator directive 2026-09-13)
- **Priority**: normal

## Reason (audit provenance)

SEC AUDIT MEDIUM (B-4 + A-5-adjacent): locked -> 500 INTERNAL_ERROR indistinguishable from crash, worker parked up to 30s, journal mode delete = readers block behind writer, 500s carry no X-Request-ID (correlation gap). Severity medium.

## Acceptance

sqlite database-is-locked maps to 503 (or contract code) with Retry-After instead of 500 INTERNAL_ERROR; per-request busy timeout shortened from 30s to a documented value; WAL enabled OR single-writer constraint documented at the config site; a GET that writes (_sweep_expired) is audited and moved out of the read path or made contention-safe; measured probe: concurrent external writer -> clients get typed retryable responses, zero 500

## Files

backend/app/errors.py backend/app/database.py backend/app/services/staff_waitlist.py backend/tests/
