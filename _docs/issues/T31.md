---
id: T31
title: Staff waitlist list paginates in SQL instead of materialising the full result
depends:[]
platform_issue: 86
---

## Metadata

- **Phase**: phase-5-audit
- **Type**: security-audit
- **Area**: audit-batch
- **Size**: M
- **Milestone**: Security audit remediation (operator directive 2026-09-13)
- **Priority**: normal

## Reason (audit provenance)

SEC AUDIT MEDIUM (B-2): limit unbounded+negative-tolerant and sliced in PYTHON after full fetch - every list call is O(rows) on a non-rate-limited staff surface; limit=-1 answers 200 empty-items-nonzero-total (silently wrong pagination). Severity medium.

## Acceptance

queue_rows applies LIMIT/OFFSET in SQL; total comes from COUNT; limit<=0 or offset<0 rejected at schema layer (see bounds issue); list latency independent of day size (measured N=200 fixture: request cost ~O(limit)); response shape unchanged

## Files

backend/app/services/staff_waitlist.py backend/app/services/queue_rows.py backend/app/routers/staff_waitlist.py backend/tests/
